/** @type {APIEndpointHandler} */
module.exports = {
    handler: function (req, res) {
        // Design use case 2.3
        // TODO: send the individual or the organization record back to frontend
    },
    method: "put",
    path: "/profile/:id"
}