/** @type {APIEndpointHandler} */
module.exports = {
    method: "put",
    path: "/events/:id",
    handler: function (req, res) {
        // Design use case 4.2
        // TODO: read user form and update an event
    }
}