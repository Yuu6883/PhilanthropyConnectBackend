/** @type {APIEndpointHandler} */
module.exports = {
    method: "get",
    path: "/events",
    handler: function (req, res) {
        // Design use case 4.4 & 4.5
        // TODO: read event feed
    }
}