/** @type {APIEndpointHandler} */
module.exports = {
    method: "get",
    path: "/organization/rate/:id", // id here is org id
    handler: function (req, res) {
        // Design use case (missing? or included in view profile)
        // TODO: read the ratings of an organization
    }
}